import ConfigRoutes from "../config/config.routes"
import HOMEPAGE from "../pages/Home"

const PublicRoutes = [
    {
        path : ConfigRoutes.homepage,
        component : HOMEPAGE,
    }
]

const AdminRoutes = [

]

export default PublicRoutes